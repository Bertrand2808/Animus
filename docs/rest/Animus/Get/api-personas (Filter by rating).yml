info:
  name: api/personas (Filter by rating)
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/api/personas?content_rating=pg"
  params:
    - name: content_rating
      value: pg
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: api/personas/:id (Success)
  type: http
  seq: 4

http:
  method: GET
  url: "{{BASE_URL}}/api/personas/{{TEST_UUID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

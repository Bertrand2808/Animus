info:
  name: api/personas (Empty list)
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_URL}}/api/personas"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

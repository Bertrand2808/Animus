info:
  name: api/personas (List all)
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/api/personas"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

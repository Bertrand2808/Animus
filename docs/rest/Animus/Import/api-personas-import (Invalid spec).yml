info:
  name: api/personas/import (Invalid spec)
  type: http
  seq: 4

http:
  method: POST
  url: "{{BASE_URL}}/api/personas/import"
  body:
    type: json
    data: |
      {
        "spec": "chara_card_v1",
        "spec_version": "1.0",
        "data": { "name": "OldName" }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

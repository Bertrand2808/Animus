info:
  name: api/personas (Missing Name)
  type: http
  seq: 2

http:
  method: POST
  url: "{{BASE_URL}}/api/personas"
  body:
    type: json
    data: '{"description": "Test", "personality": "..."}'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
